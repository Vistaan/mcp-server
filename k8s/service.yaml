apiVersion: v1
kind: Service
metadata:
  name: workflow-os-mcp
  namespace: workflow-os
  labels:
    app.kubernetes.io/name: workflow-os-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: workflow-os-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
