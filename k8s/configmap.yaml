apiVersion: v1
kind: ConfigMap
metadata:
  name: workflow-os-mcp-config
  namespace: workflow-os
  labels:
    app.kubernetes.io/name: workflow-os-mcp
data:
  MCP_TRANSPORT: "http"
  MCP_PORT: "${PORT}"
  NODE_ENV: "production"
